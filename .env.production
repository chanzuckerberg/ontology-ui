# Environment variables for the "prod" (production) environment build.
# Remember, these will be embedded inside the JS bundle, so don't put
# anything secret here.
PUBLIC_URL=https://labs.cellxgene.cziscience.com/ontology-ui
